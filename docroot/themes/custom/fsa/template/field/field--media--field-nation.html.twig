{#
/**
 * @file
 * Theme override for a nation field in media document (section) to be heading.
 */
#}
{% for item in items %}
    <div role="heading" {{ attributes }}>{{ item.content }}</div>
{% endfor %}
